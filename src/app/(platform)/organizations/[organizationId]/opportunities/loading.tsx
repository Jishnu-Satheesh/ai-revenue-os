/**
 * The address redirects immediately, so there is no content to sketch.
 * A skeleton here would promise a feed that never paints.
 */
export default function OpportunitiesLoading() {
  return null;
}

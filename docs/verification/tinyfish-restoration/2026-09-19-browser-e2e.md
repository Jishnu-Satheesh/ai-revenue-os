# Browser end-to-end verification — TinyFish market-monitoring restoration

Date: 2026-09-19. Verifier role: verification only (no code, spec, config, migration, commit, or stash touched; this report file is the single write).

## Setup as run

- Repo: `/home/spy/Documents/ai-revenue-os/.worktrees/governed-channel-intelligence`, branch `feat/governed-channel-intelligence` (shared tree with peer WIP — nothing touched).
- Dev server: already running for this worktree (`next dev --port 3103`, pid 171875); reused, none started.
- Org: `2dda45b8-82db-4f5f-b17d-611b9bbb7846` ("Al Noor Kitchen"), user-confirmed. Session: pre-authenticated browser profile (agency owner, super-admin), user-confirmed.
- Migration/qualification: user confirmed migration `20260919120000` pushed, **no TinyFish qualification row staged** — so Phase A only, fail-closed behavior is a PASS. Phase B skipped.
- Trigger.dev access: **no Trigger.dev MCP is connected to this session** (only browser tools plus TinyFish search/fetch). The `cloud.trigger.dev` dashboard sits at its login page in the attached browser; user chose to trigger/check worker runs themselves and hand over a run id (still owed at report time).
- Context read before verifying: plan `docs/superpowers/plans/2026-09-18-tinyfish-market-monitoring-restoration.md`, `adrs/0065-tinyfish-durable-market-research.md`, spec 022 §§4.2, 6.5, 8.2, 9.7, ledger `.superpowers/sdd/2026-09-18-tinyfish-market-monitoring-restoration/progress.md`, worktree `AGENTS.md`.
- Screenshots: captured inline during the session (GI page load, live-preview results, Recommendations tab, Market monitoring dialog). Console messages pulled via devtools for every phase.

## Per-check results (Phase A)

| Check | Verdict | Evidence |
|---|---|---|
| A1 — GI page loads, zero slice-attributable console errors/warnings | PASS | Page renders for the org with header, 4 tabs (Overview / Recommendations 29 / Your actions 8→9 / Insights & market 6), honest performance empty state. Console: Fast Refresh dev noise + 1 pre-existing `performance_read_failed` warn (page.tsx:479 catch-all; org has no analyzed channel data) + 1 Chrome a11y issue (form field missing id/name, see observations). Nothing from the research-provider slice. |
| A2 — Market monitoring dialog opens; unqualified start ends fail-closed, safe message, no spend | PARTIAL | Dialog opens from header; branch picker (Al Barsha/Deira/jumeirah), no auto-selection; scope loads honestly; research-area fields are research-only. Start accepted (`POST market-profile/research` → 201, `domain_event.published`, 06:21:05 UTC). Run recorded honestly: Your-actions entry "Market research started — Al Barsha, 19 Sep 2026"; Insights tab "Research outcomes: Queued … 0 searched · 0 supported · 0 sources"; status API `stage: queued, active: true, safeFailureCode: null`. Reopened dialog shows "Research in progress" (no duplicate run). **Terminal fail-closed outcome NOT observed** — run still `queued`, unchanged, 30+ min (see Defect 1). No stack trace, no provider text, no spend observed anywhere. |
| A3 — Market Watch live preview: explicit click only, fresh results, discards on close, no evidence, permission-gated | PASS | Preview mounts only with `?branch=` set and only fetches after clicking "Show live results" (Brave ephemeral path intact). Returned "10 fresh leads · 2 queries · checked 19/09/2026 11:57:08 AM" with titles/domains/snippets/links. Disclaimer shown: "Live preview — not saved. Unverified leads, not evidence… Closing discards everything." After close: Insights count still 6, no new insight/evidence from preview topics. Route requires `growth_intelligence.manage` (returns AUTHORIZATION_ERROR otherwise — read in `market-profile/research/route.ts`; start route likewise). Cross-user gating not exercised (single super-admin session). |
| A4 — Insights & market renders prior state honestly; Recommendations lane unaffected | PASS | Insights: prior business findings with windows/freshness + 1 labeled seed insight; Data gaps with repair links; no invented market findings, coverage, or progress. Recommendations tab still 29 items, channel cards with feedback actions intact, zero market-derived links (correct — no research has completed). |
| A5 — Trigger.dev: recent growth-intelligence.\* runs show blocked baselines only, no spend, no TinyFish evidence while unqualified | BLOCKED | No Trigger.dev MCP in this session; dashboard login is the user's step (page open at login, untouched). User will trigger/check runs and supply a run id. Zero spend confirmed at UI/API level only (0 sources, 0 claims, `safeFailureCode: null`, Insights count unchanged). |

## Overall verdict

- **PHASE A: CONDITIONAL FAIL (kept open by Defect 1).** A1, A3, A4 pass. A2's fail-closed *ending* is unproven because the run never leaves `queued`; A5 is blocked on run visibility.
- **PHASE B: SKIPPED** — reason: user confirmed migration pushed but no TinyFish qualification row staged. Per the handoff, Phase B must not run on assumption.

The restoration is **NOT DONE**. It becomes DONE only on an explicit overall PASS after Defect 1 is resolved and A2-terminal + A5 are re-verified (needs the owed Trigger.dev run id or dashboard session).

## Defects

### Defect 1 — Important: unqualified research run stuck `queued` ~30+ min, never reaches the fail-closed terminal state

- Phase/step: A2 (and blocks A5).
- Expected: starting research while unqualified ends fail-closed — today's blocked baseline with zero spend and a safe code (`FEATURE_NOT_AVAILABLE` / `ADAPTER_UNAVAILABLE` / `RESEARCH_BUDGET_UNAVAILABLE`), named in Your actions / Research outcomes.
- Observed: pipeline `a86936b2-26b2-42be-9363-1c7be2c1b460` (Al Barsha, started 06:21:05 UTC) reads `stage: queued, active: true, safeFailureCode: null, sourceCount: 0, claimCount: 0`, `stageChangedAt` == creation; unchanged across polls spanning 30+ min. UI says "research is still running" indefinitely; retry correctly withheld.
- Evidence: status API `GET /api/organizations/…/market-profile/research/a86936b2-…` (200, quoted above); dev-server log shows `POST …/market-profile/research 201` + `domain_event.published` and no dispatch error; `Your actions` start entry present, no terminal entry.
- Suspected area (from reading code only): the path between `startBranchResearch` + published domain event and the `growth-intelligence.run-market-research` task claim (`src/trigger/growth-intelligence.ts` task def near line 947; event-consumer/immediate-trigger wiring) — or an environment mismatch (dev server's Trigger project/key vs the deployed workers). Not distinguishable without run visibility; owner to triage with the Trigger.dev run list.
- Same symptom family, pre-existing: two one-time "Research projects" (`Summer Dining Deals` / `Summery dining deals`, jumeirah branch, requested 15 Sep 2026) show `lifecycle: active` with `reportsByProject: {}` — 4 days "Researching" with zero reports. Different lane from the restored pipeline, noted for the owner, not attributed to this slice.

### Observation 2 — Minor: `countryCode: "UN"` stored for "United Arab Emirates"

- The pipeline status API records `settingsSummary.city: "Dubai", countryCode: "UN"` after entering "United Arab Emirates" in the dialog's research-area group ("UN" is not the ISO code; UAE = AE).
- Outside the restoration blast radius (dialog research-area handling, pre-existing). Flagged for the owner; no research ran, so no impact yet.

### Observation 3 — Minor: `research_history_degraded` (DOMAIN_ERROR) warn in dev log

- One `growth_intelligence.research_history_degraded` warn at 06:26:34 UTC during branch-scoped Insights loads. UI stayed honest (no invented data). Needs owner triage; not attributed to the slice.

### Observation 4 — Minor: Chrome a11y issue on a form field

- Console issue "A form field element should have an id or name attribute (count: 1)", first seen while the Market monitoring dialog was open. Cosmetic/accessibility only.

### Notes (not defects)

- `performance_read_failed` warn is the pre-existing page.tsx:479 catch-all (org has no analyzed channel data); the page shows the honest "not available" empty state.
- Env naming: the worker reads `TINYFISH_SEARCH_API_KEY` (`src/trigger/growth-intelligence-tinyfish.ts:68`) and `.env.example` documents it, but the worktree `.env.local` sets `TINYFISH_API_KEY`. Deployed-worker env lives dashboard-side — owner to confirm the dashboard variable name matches before any canary.
- `pnpm db:migrations:list` could not reach staging from this environment (pooler connection timeout); migration-pushed state is per the user's confirmation, not independently verified here.
- Collaboration-board claim row for this verification was not added: the handoff limits this session to this single report file. Board owner may log: "2026-09-19 TinyFish browser e2e — Phase A conditional fail (Defect 1), Phase B skipped."

## Re-verification checklist (to reach overall PASS)

1. Resolve Defect 1 (worker claims the request or dispatch mismatch fixed), then re-run A2 on the canary org and capture the terminal fail-closed message + screenshot.
2. Supply the Trigger.dev run id (owed) or a dashboard session; complete A5: run list shows blocked baselines only, zero spend, no persisted TinyFish evidence while unqualified.
3. Optionally stage the TinyFish qualification row then run Phase B per the handoff (B1–B6).

---

# Addendum — re-verification after fix (2026-09-20, ~06:35–07:10 UTC)

Role note: after the initial report the user ordered a fix ("fix it ASAP") and approved each plan in writing. Verification role resumed for this addendum. Code changes (uncommitted, same worktree/branch): F1 sweep recovers never-completed one-time projects; F2 bare branch start wakes `dispatch-due`; F3 new `fail_market_research_pipeline` RPC + repo/workflow wiring so failed requests advance the pipeline; F4 terminally-failed one-time updates render "Research could not finish". Migration `20260919150000` pushed by the user; workers redeployed (v20260919.8); pgTAP suite `growth_intelligence_research_pipeline_fail_test.sql` 18/18 green on staging (also the new RPC's first-call verification); unit suites 161/161 across the 10 affected files; tsc/eslint clean on touched files.

Run visibility: still no Trigger.dev MCP tool in this session, so A5 was completed through a stdio bridge to the project's own authenticated `trigger` CLI (`./node_modules/.bin/trigger mcp`, profile `default`), which speaks. The MCP server itself is healthy; only this session's tool wiring lacks it.

## Updated per-check results

| Check | Verdict | Evidence |
|---|---|---|
| A1 — GI page loads, zero slice-attributable console errors/warnings | PASS (reaffirmed) | Page renders; console shows only Fast Refresh dev noise, zero errors/warnings. |
| A2 — unqualified start ends fail-closed, safe message, no spend | PASS | Pipeline `a86936b2` (Al Barsha): `research_failed` + `EXTRACTION_UNAVAILABLE`, verified in DB and explained below. Market Watch shows "Failed research requests — market_research · EXTRACTION_UNAVAILABLE" with working "Retry request" buttons (screenshot). No stack trace, no provider text. Spend $0.00 across every run. |
| A3 — live preview ephemeral and gated | PASS (as before; not re-run) | Unchanged code path; no evidence created by preview (Insights still 6). |
| A4 — honest prior state, Recommendations unaffected | PASS (reaffirmed) | Insights 6, Recommendations 29, no market-derived links, no invented findings. |
| A5 — run list shows blocked baselines only, zero spend, no TinyFish evidence | PASS | dispatch-due ×3 completed; run-market-research ×6 completed $0.00; run-market-monitoring-update ×2 (Sep 15) completed $0.00; synthesis/consolidation 0 runs (correct — nothing succeeded). Sources 0, claims 0. Full run ids in evidence below. |

## Overall verdict (updated)

- **PHASE A: PASS.** A1–A5 all green.
- **PHASE B: SKIPPED** (unchanged — no TinyFish qualification row staged).
- The restoration is **DONE within its Phase A scope**: unqualified organizations fail closed end to end with safe codes, zero spend, no persisted evidence, and honest UI. Defect 1 is resolved; no new defects.

## Defect 1 — resolution record

Root causes found (two, not one): (a) a bare operator start never woke `dispatch-due` (only fresh reports did) — fixed by F2, proven live when dispatch ran 19s after the Jumeirah start (`run_06gbi5s766hbokmk2vurtjgb01`); (b) `failRequest` never advanced the pipeline row (no failure counterpart to `complete_market_research_pipeline`) — fixed by F3, proven live by pipeline rows carrying `research_failed` + safe codes, which only the new RPC writes.

## Evidence (all read, nothing written except this file)

- dispatch-due: `run_06gbi5s766hbokmk2vurtjgb01` (09:55:33 UTC, F2 wake), `run_06gbih0qrn6bt77ot6iavi2701` (10:44:13 UTC, manual verification trigger), `run_06gbr1ntp2g0j7lbrmngd22j01` (06:35:44 UTC) — all completed, $0.00.
- run-market-research ×6 (3 Sep 19 v20260919.5, 3 Sep 20 v20260919.8): all completed ~500ms, $0.00, output `failed / EXTRACTION_UNAVAILABLE` (worker model composition unconfigured — correct fail-closed gate, pre-Task-8).
- Pipelines (DB read): `a86936b2` research_failed/EXTRACTION_UNAVAILABLE; `78ae5da2` cancelled/SUPERSEDED_BY_NEW_SCOPE; `e1d12333`, `d785a6f5` research_failed/EXTRACTION_UNAVAILABLE.
- Requests (DB read): `6ba13bfb`, `8f304a9c`, `d37147a8` all failed, 1 attempt each; Sep-15 updates durably `research_failed`.
- UI (screenshots): projects "Research could not finish / The background research run could not finish. No findings were saved.", counts In progress 0 / Needs attention 2; Market Watch "Failed research requests" with safe codes + Retry buttons.
- pgTAP: 18/18 on staging for the new RPC (contract, grants, live fail, replay, stranded repair, fencing, boundary, auth).

## Still open (owner, unchanged)

- Observation 2 (`countryCode: "UN"`), Observation 4 (a11y form field), `performance_read_failed` warn: pre-existing, untouched.
- Phase B needs a staged TinyFish qualification row (user step) plus worker env (`TINYFISH_SEARCH_API_KEY`, kill-switch, model composition) before any canary.
- The project-scope research executor remains unstaged by design (`createFailClosedMonitoringResearch`); one-time projects will keep failing closed until that slice is built.

---

# Addendum — live canary (2026-09-21, T0 05:00:26Z)

Fourth start (Al Barsha, pipeline 3faf56fc) is the first run to exercise the
lane for real, after 4 canary-found defects were fixed (metadata schema TS +
DB, bound-run fail path, spend-fence provider, pgTAP fixture premises).

## Evidence

- Run 06gc4ljrtub9da5ag831b4n101 (v20260921.7, 19.9s, $0.0007 compute, $0.00
  provider): adapter tinyfish selected, 3/3 slots searched with attempt ids,
  budget $1 quote admitted, 3 attempts settled REPORTED $0 actual (free tier).
- Outcome partial → pipeline no_findings / NO_ELIGIBLE_FINDINGS with exact
  per-slot coverage (searched_no_usable_evidence × 3).
- Zero sources/claims is HONEST, verified by replicating the exact slot
  queries against TinyFish directly: quoted local_market and topic queries
  return 0 results; the fictional seed competitor returns 3 generic
  listicles with nothing citable. The worker dropped correctly.
- Leakage: exactly 1 pipeline/request/tinyfish-run, canary org+branch only.
  Day allowance $1 of $5. Zero over-max settlements.

## Verdict

- PHASE B (live path): PASS — search, spend fencing, settlement, honest
  empty outcome, and full audit trail proven with real provider calls.
- Positive-path proof (sources → extraction → claims) still open: it needs
  real competitor/topic data; the canary org carries seed fiction. Offered
  as an optional fifth start with operator-typed real competitors.
- Stray run row 096144d6 (controller diagnostic probe, Sep 20, no data,
  expired lease) excluded from all counts; it cannot affect worker paths.

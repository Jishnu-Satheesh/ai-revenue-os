- **Independent Creative Studio Implementation Plan — PROPOSED, NOT APPROVED.** Updated 2026-09-21. This file authorizes no code, migration, paid generation, deployment or staging mutation. User decisions settle product direction; the complete spec and this execution plan still need approval.
- **Agent workflow:** use `subagent-driven-development` task by task after approval. The user's explicit workflow overrides skill templates: preserve review evidence, repeat fixes/review until clean, do not declare unresolved failures complete, and keep this plan bullet-only as AGENTS.md requires. No implementation is performed by this design task.
- **Goal:** organization-owned Studio with optional campaign, durable history, complete AI posters including exact supplied Text Copy, genuine progressive image frames, original-model contextual marker edits, download and exact selected-byte Campaign handoff.
- **Spec:** `docs/superpowers/specs/2026-09-20-independent-creative-studio-design.md`; contracts: `docs/design/creative-studio-independent/technical-contract.md`; evidence: `current-studio-audit.md` and `provider-and-assets-audit.md` in that folder. If a parent document is still being drafted, read its final approved revision before Task 1.
- **Architecture:** standalone organization documents/versions/runs and private continuations; qualified streaming provider behind a typed port; Postgres owns admission, leases, idempotency and linkage. Selected campaign receives a pending selection or an exact unreviewed deliverable; no auto-publication, media reservation or hidden campaign generation.
- **Stack:** repository Next.js/TypeScript strict/Zod/TanStack Query + Form/shadcn; hosted Supabase/Postgres/private Storage; Trigger.dev worker; provider SDK selected by measured qualification. No local Supabase, Docker database, `pnpm db:types`, stash, push or repository-wide formatting. Prefix shell commands with `rtk`.
- **Global acceptance:** Campaign optional; Studio history saved; complete poster with verbatim Text Copy; at least one independently decodable non-final progressive frame visible before final in the qualification and browser acceptance runs; same provider/exact model and original context for marker edits; all final outputs immutable; references and data tenant-scoped; exact selected bytes ultimately presented for Campaign review; no hidden generation when creating a campaign.
- **Open assumptions proposed for approval:** Instagram Feed default 4:5/1080×1350 export; square and story presets; maximum three design references, three products, two marks further limited by profile; eight markers; effective upload limits min(existing bucket/organization, application 15 MiB, qualified profile including encoding overhead)/intersected 200–8000 px; 24-hour preview cleanup/30-day event retention; explicit administrator-set preparation budget; new Studio permission mapping in technical contract. Changing these materially requires plan reconciliation before implementation.
- **Blast radius:** additive Studio tables, permissions, private buckets and worker; new organization route/sidebar entry; existing Campaign deliverable source/render-input schemas and source resolution; draft creation/linking and setup reconciliation; existing Asset Library/brand readers only where adapters require. Preserve old compositor output/digests and existing Campaign approval/publication gates.
- **Risk and rollback:** provider partial-frame/context/quality limitations may block feasibility; strict qualification first. Additive migrations stay in place on rollback; feature allowlist hides new navigation/admission and stops new runs, while old version/download/review readers remain available. Never rewrite legacy reviews, output bytes or old digests. Halt leases safely; reconcile potentially paid unknown outcomes rather than replaying them.
- **Per-task review gate G:** controller captures baseline diff/file ownership, dispatches one fresh implementer with exact task/approved spec/contracts, then fresh independent spec reviewer and code reviewer. Reviewers may run in parallel read-only; only one source writer operates at a time. Frontend verifier uses Chrome DevTools MCP on the actual app. Backend-only tasks verify a real browser seam when wired; otherwise record “browser seam not yet wired—deferred to Task N,” never UI PASS. A fixer addresses findings, covering tests rerun, reviewers recheck changed scope, browser verifier repeats affected flows; continue until clean. Security/data-loss/acceptance failures cannot be parked as complete. Material scope changes return for explicit approval.
- **Evidence:** retain `.superpowers/sdd/2026-09-20-independent-creative-studio/progress.md`, `task-N-brief.md`, `task-N-implementation.md`, `task-N-spec-review.md`, `task-N-code-review.md`, `task-N-browser.md`, and numbered fix reports. Include exact commands, result counts, commit/diff identity, screenshots, run/version IDs and redacted DB assertions; no secrets/raw provider history. Never delete this evidence as skill cleanup.

- [ ] **Task 0 — Approval, reference freeze and environment gate. Depends on no implementation.**
- Files: finalize approved spec/technical contract/this plan; allocate an unused ADR number against current tree and collaboration board, then draft `adrs/<allocated-number>-independent-full-poster-studio.md` before implementation approval, explicitly superseding ADR 0042/Spec 020 only for the new full-poster workflow and preserving legacy compositor guarantees. Draft scoped Spec 019/020 and ADR 0049 reconciliation of stale delivery status and the new boundary in the same approval package; do not mark existing proposed ADRs accepted by inference. Record the allocated exact ADR path in this plan and each implementation brief before Task 1. Create `docs/design/creative-studio-independent/reference-manifest.json`, `docs/verification/creative-studio-independent/qualification-plan.md`, and progress ledger. Record existing `rtk git status --short` and narrow baseline patches without stashing; peer changes remain untouched. Claim intended files on collaboration board before implementation.
- Confirm user-approved original wireframe/PNG files are actually available; record immutable paths, byte sizes, SHA-256, dimensions and approval date. Preserve the originals read-only; implementation screenshots use separate paths. If attachment is missing, record reference-missing and request it, or accept the prototype only if the user explicitly approves it as the replacement reference. Freeze the approved replacement with that decision recorded; never silently substitute a generated reference or claim parity to the unavailable sketch.
- Verify Chrome DevTools MCP, authenticated test organizations A/B and roles, Trigger.dev MCP/dev runner, hosted staging connection, private storage, provider credentials/access and explicit authorized qualification budget. Read relevant Trigger/Supabase skills. Record available package versions and actual migration head without printing credentials. Allocate collision-free migration timestamps/ADR number before using proposed filenames below; any rename is recorded in ledger.
- Define bounded qualification matrix and cost cap before provider calls; empty credentials or absent cost authorization blocks only dependent provider steps, not read-only preparation. No implicit nonzero budget policy or live organization settings seeded for convenience.
- Acceptance: approved spec+plan+scoped ADR/reconciliation revision hashes, known original reference hashes, current dirty baseline, test tenant fixtures and provider access/budget evidence. Run gate G for preflight/spec consistency; no fake generation/browser pass. Tasks 1 and 2 may proceed only after relevant approval; no production build depends on an unqualified profile.

- [ ] **Task 1 — Provider qualification harness. Depends on Task 0 provider authorization.**
- Create `scripts/creative-studio/qualify-provider.ts`, `src/modules/creative-studio/infrastructure/provider-qualification.ts`, `src/modules/creative-studio/infrastructure/provider-qualification.test.ts`, `docs/verification/creative-studio-independent/provider-qualification.md`; change `package.json`/`pnpm-lock.yaml` only if a proven SDK change is necessary and within approved scope.
- Consume proposed `StudioProviderProfile`/event contract; produce measured profile evidence, not enabled production defaults. Inspect current primary provider docs and SDK. Test Gemini; qualify alternate provider/API when it lacks real progressive frames. Provider/model choice must satisfy partial images AND same-model continuation; never trade one requirement away silently.
- Record timestamp/type/index/hash/dimensions for each actual independently decoded preview and final, without exposing image/customer payloads in logs. Generate 4:5, 1:1 and 9:16 with supplied copy/product/design/logo, then fresh-process reload and two contextual edits including a branch from an older version. Include typographic/multilingual fixtures representative of intended usage; human inspect spelling/logo/product fidelity.
- Test unknown timeout versus definite refusal, malformed/oversized frames, interruption, expired continuation and unavailable pinned model. Prove opaque context replay without user-visible/raw stored hidden reasoning; document provider retention/deletion behavior. Establish max serialized request size, image count and native/export dimensions.
- Acceptance: provider evidence demonstrates real pre-final displayable refinement, final complete poster, continuity after reload, safe failure boundaries and measured costs within authorized cap. Any missing requirement keeps feasibility failed and profile disabled. Gate G; browser verifier displays the actual frames through a minimal local evidence viewer or reports app seam deferred to Task 8, never calls a replay UI production streaming.

- [ ] **Task 2 — Pure contracts and compatibility fixtures. Depends on approved Task 0 design; provider-specific profile finalization depends on Task 1.**
- Create `src/domain/creative-studio/schemas.ts`, `provider.ts`, `events.ts`, `policy.ts`, `campaign-link.ts`, `index.ts` and matching `.test.ts` files. Create `src/modules/creative-studio/index.ts` with explicit supported exports only. Modify `src/domain/campaigns/deliverable.ts`, `deliverable.test.ts` and `deliverable-identity.test.ts` for source and versioned input union.
- Implement schemas/signatures in technical contract: exact non-trimmed Text Copy, limits/markers/references, stage transitions, safe errors, immutable profile binding, `studio_version` source and v2 `studio_full_poster` render inputs. Add operation-specific prompt validation (blank saved draft/New Idea allowed, Enhance requires text), primary manifest referenceId membership (including upload-only Exact design), code-point-range channel-logo consent manifest, typed persisted suggestion result and versioned export transformation/source identity. Keep legacy v1 untagged object canonicalization identical; reject cross-arm source/input combinations and hash mismatches. Suggestions cannot mutate copy.
- Tests: literal Unicode/newline copy preservation, blank New Idea and persisted suggestion reload, explicit logo-substitution range/phrase/overlap checks, selected primary-reference membership, export identity/digest changes, invalid marker coordinates/ordinals, provider-cap limits, rejected-reference exclusion, parent/profile mismatch; fixed pre-change legacy fixtures produce byte-identical `deliverableRenderDigest` strings; v2 changes on source/hash/copy/reference change. No node-only modules reachable from client exports.
- Acceptance: focused domain suites and client-module boundary test pass; gate G. Browser applicability is schema-only until Task 7; report that limit explicitly.

- [ ] **Task 3 — Studio schema, permissions, policy and run lifecycle. Depends on Tasks 1–2.**
- Create proposed `supabase/migrations/20260921130000_independent_creative_studio.sql`, `supabase/tests/independent_creative_studio.test.sql`; modify `src/domain/access/permissions.ts`, its drift tests, `src/lib/supabase/database.types.ts` narrowly. Create `src/modules/creative-studio/infrastructure/repository.ts` and `repository.test.ts` for typed RPC mapping.
- Implement documents/versions/exports/runs/events/uploads/previews/continuations/policies, private storage boundaries and RPCs from technical contract except Campaign linkage RPCs reserved for Task 9. Validate JSON inside database; tenant composite FKs; continuation hidden from authenticated SELECT; grants and audit events explicit. Completion supports no campaign yet and records bounded deferred link intent to be wired in Task 9 without reporting successful linkage prematurely.
- Tests: A/B tenants; viewer read but no writes/spend; operator explicit bounded generation; administrator-only policy; foreign references, duplicate idempotency/different digest, concurrent admissions, policy reservation ceiling, single active image run, stale revision/current pointer, expired lease, cancel/complete races, unknown paid outcome and replay without double reservation. SQL insert/update/delete grants fail outside RPCs.
- Validation: review existing hosted schema first, migration dry-run, approved forward push, then hosted pgTAP and explicit first invocation of every new table-reading PL/pgSQL with authorized fixtures. Record actual results/cleanup. No local reset/type generation. Gate G; browser permission seam deferred to Task 7, DB/API evidence does not claim UI success.

- [ ] **Task 4 — Authorized intake and source manifest. Depends on Tasks 2–3.**
- Create `src/modules/creative-studio/application/reference-service.ts`, `infrastructure/reference-reader.ts`, `infrastructure/upload-intake.ts` and matching tests; create routes `src/app/api/organizations/[organizationId]/studio/uploads/route.ts` and `uploads/[uploadId]/complete/route.ts` with `uploads/routes.test.ts`.
- Reuse authoritative Creative History eligibility, product, Brand Kit readers and safe byte-intake helpers; reserve→transfer→verify/finalize upload flow. Create signed reservations only for caller's upload; service worker verifies bytes/hash, expiry, MIME/dimensions/orientation, rights attestation and total profile capacity. Fresh upload is usable as explicitly authorized current reference, never silently approved history.
- Tests: rejected/unreviewed history excluded at real provider-input builder; review revoked between pick/dispatch; foreign upload/object ID; poisoned MIME, decompression bomb, EXIF coordinates, effective per-file/encoded-request limit overflow, expired reservation, duplicate finalization and missing rights; expired preview signs anew without changing identity. Preserve original source records.
- Acceptance: route/repository/intake tests and hosted storage policies pass. Gate G; Chrome verifier uses real upload seam once Task 7 picker is mounted, records pending browser gate otherwise.

- [ ] **Task 5 — Durable generation, continuation and previews. Depends on Tasks 1–4.**
- Create `src/workflows/creative-studio/generate.ts`, `contracts.ts`; `src/trigger/creative-studio.ts`; `src/modules/creative-studio/infrastructure/provider-adapter.ts`, `continuation-store.ts`, `output-intake.ts`, `run-dispatch.ts`; corresponding unit/workflow tests. Add necessary environment keys to `src/lib/env.ts`/`.env.example` without secrets; provider model/profile config is pinned per session.
- Implement qualified generate/edit streaming port, DB claim/heartbeat, reference revalidation, bounded provider call, independently decoded partial-frame persistence, final intake/hash, private continuation, transaction completion and reconcilable unknown failures. Preserve exact Text Copy in provider request and receipt; do not invoke deterministic poster compositor in this path. Marker request uses clean parent plus numbered overlay and original selected-version context.
- Tests: valid final without preview saved with missing_progressive_preview anomaly (does not pass progressive acceptance), malformed frame rejected, final event only after durable commit, late worker fenced, client disconnect does not pay twice, cancellation settled, parent immutable, old-version branch, same-model enforcement, opaque context not logged/leaked, continuation expired without fallback, no image/refusal distinct from timeout. Suggestion operations persist typed result_json and return it only through authenticated scoped reads; no image version is created. New Idea accepts blank input; Enhance requires nonblank. Both spend within policy and do not mutate Text Copy.
- Acceptance: mocked deterministic workflow tests plus authorized real worker qualification using already bounded Task 1 budget or separately approved extension. Report stored run/version/frame/continuation evidence, not just Trigger COMPLETED. Gate G; browser full streaming validation reserved to Task 8.

- [ ] **Task 6 — Scoped application/API and resumable event stream. Depends on Tasks 3–5.**
- Create `src/modules/creative-studio/application/service.ts`, `route-handlers.ts`, `infrastructure/route-wiring.ts`, `infrastructure/event-reader.ts` and tests. Create root API `src/app/api/organizations/[organizationId]/studio/route.ts`, `documents/route.ts`, `documents/[documentId]/route.ts`, `policy/route.ts`, `runs/route.ts`, `runs/[runId]/route.ts`, `runs/[runId]/events/route.ts`, `runs/[runId]/cancel/route.ts`, `versions/[versionId]/download/route.ts`, `versions/[versionId]/exports/route.ts`, `exports/[exportId]/accept/route.ts`, `runs/[runId]/suggestion/route.ts`, and `routes.test.ts`.
- Export `createStudioService`, `readStudioWorkspace`, `admitStudioRun`, `readStudioRunEvents` and `signStudioVersionDownload` from module public API. User paths use session-scoped clients only. SSE event cursor is DB sequence; reauthorize connects/heartbeats, snapshot then replay/deduplicate. Downloads resolve selected final/export identity and hash, never previews or raw client paths. Persist versioned export transforms in studio_exports and preview contain/pad or explicit reviewed crop; native artifact stays immutable. A pending export acceptance defers Campaign auto-link; accept route pins exact export hash and applies requested link atomically after permission revalidation, never attaches native pixels prematurely. Implement initial format table (Instagram Feed/Stories, Google square/horizontal/vertical, generic ecommerce), actual MIME/encoding ceilings and capability refusal; no Amazon-ready claim. Suggestions expose exact result through the scoped suggestion route, not SSE. Budget policy UI stays an admin-only inline/organization-controls seam, not a new standalone settings page.
- Tests: foreign UUIDs and role denial for every endpoint; CSRF/session conventions; reconnect after terminal completion; retention-expired cursor returns snapshot/restart marker; revoked membership terminates stream; 409 idempotency conflict; stale parent rejection; safe errors; signed URL expiry and selected-export byte-hash equality on download; derivative replay, pad/crop consent, transform hash/dimension/MIME limits, and rejected unsupported placement; persisted suggestion reload with no extra paid request. SSE reconnect does not dispatch generation.
- Acceptance: endpoint/integration suites and authenticated browser network inspection against actual run state; no paid run needed for denial tests. Gate G. UI interaction full gate follows Task 8.

- [ ] **Task 7 — Studio page, navigation, settings and saved history. Depends on Tasks 4 and 6 plus frozen visual reference.**
- Create `src/app/(platform)/organizations/[organizationId]/campaigns/studio/page.tsx` and `src/app/(platform)/organizations/[organizationId]/campaigns/studio/[documentId]/page.tsx`; `src/components/creative-studio/studio-workspace.tsx`, `studio-settings.tsx`, `reference-picker.tsx`, `studio-history.tsx`, `studio-canvas.tsx`, `studio.module.css`, matching component tests; modify `src/components/layout/sidebar.tsx`/`sidebar.test.tsx` only for Campaigns group order Overview→Creative Studio→Asset Library; static Studio path must resolve independently of [campaignId].
- Build approved 20% settings / 80% history-or-canvas desktop composition with existing shadcn primitives; responsive settings drawer/panel, actual saved history, optional campaign picker, approved design/upload references, Exact design/Take Inspiration, prompt Enhance/New Idea, untouched Text Copy plus explicit channel-logo substitution chips/preview, selected primary layout reference, Instagram Feed default and all initial capability-qualified presets, export fit/pad preview, product picker and primary Generate. Query keys include organization; mutations use targeted invalidation.
- Tests: optional campaign omitted from generation; initial sidebar entry shows no selected canvas; static/detail routes and sidebar active state correct; typed copy unchanged, logo substitution explicit; suggestion accept/reject; missing rights; default explicit aspect; history reload and selection; permission-based action visibility backed by API denial; empty/loading/failed/expired previews; keyboard focus and narrow layouts.
- Acceptance: Chrome MCP desktop and mobile screenshots compared to frozen originals at matching dimensions; interaction and console/network inspection; no placeholder/demo data passed as real output. Gate G; pending live generation requirement remains Task 8.

- [ ] **Task 8 — Real streaming canvas, contextual markers and download. Depends on Tasks 5–7.**
- Create `src/components/creative-studio/use-studio-run.ts`, `marker-editor.tsx`, `generation-progress.tsx`, `version-inspector.tsx` and tests; wire `studio-workspace.tsx`, `studio-canvas.tsx` and `studio-history.tsx`. Create `e2e/creative-studio.spec.ts` with reusable authenticated support from `e2e/support/authenticated.ts`.
- Show actual independently displayable previews as they arrive; label preview and withhold export/review until validated final. Reconnect restores latest frame/run without new admission. Pin edits to selected version/profile/context; warn that whole poster may change; show copy/OCR discrepancies as review aids. Full-size inspection, saved versions and exact-byte download remain usable on context expiry.
- Tests: mock stream out-of-order/duplicate/expired frame events; actual authorized generation yields pre-final frame visible in Chrome and then durable final; reload reopens same version; two marker edits use saved context and exact same model; edit older version branches; download bytes match final hash; failed/rejected/cancelled/unknown states retain recoverable history.
- Acceptance: Chrome MCP evidence includes actual frame timestamps/hashes, final DB run/version, refresh, marker coordinates and download hash. User-visible text progress alone FAILS. Gate G; no screenshot substitution for model continuity proof.

- [ ] **Task 9 — Campaign selected-creative persistence and draft+link. Depends on Tasks 2–3 and 6.**
- Create proposed `supabase/migrations/20260921140000_studio_campaign_selected_creatives.sql`, `supabase/tests/studio_campaign_selected_creatives.test.sql`; `src/modules/campaigns/application/studio-selection-service.ts`, `infrastructure/studio-selection-repository.ts` and tests; routes `src/app/api/organizations/[organizationId]/studio/versions/[versionId]/campaign-links/route.ts` and `create-campaign/route.ts`. Extend Studio completion/repository for automatic selected-campaign link intent.
- Implement selection/draft+link RPCs and `campaign_selected_creatives` exactly as technical contract. New draft contains only supplied legitimate values and selected immutable creative; no bundle/direction fabrication. DO NOT call `createCampaignService.create` unchanged: it enqueues generation. Existing campaign selection validates `campaign.edit`; new draft requires `campaign.create`; references remain same tenant.
- Tests: automatic link after generation or deferred conversion acceptance, no pre-acceptance wrong-hash link, explicit exportId/hash through attach/create, draft creation+link atomic rollback, exact same version/hash, idempotent replay, no model/task/enqueueGeneration invocation, foreign campaign refusal, revoked permission yields visible blocked link while keeping completed poster, missing bundle yields awaiting_campaign_setup, no inherited approval or media reservation.
- Validation: hosted migration dry-run/push/pgTAP and first PL/pgSQL calls, grants/types narrow update; gate G. Browser uses explicit attach/create buttons once Task 11 mounts them, pending until then.

- [ ] **Task 10 — Resolve selected bytes into real unreviewed deliverables. Depends on Tasks 2 and 9.**
- Create proposed `supabase/migrations/20260921150000_studio_deliverable_sources.sql`, `supabase/tests/studio_deliverable_sources.test.sql`; `src/modules/campaigns/application/selected-creative-resolver.ts`, `infrastructure/deliverable-source-reader.ts`, matching tests. Modify `application/deliverable-service.ts`, `infrastructure/deliverable-repository.ts`, their tests, plus `src/workflows/campaigns/generate-bundle.ts`/tests, `src/modules/campaigns/infrastructure/repository.ts`/tests (`createCampaignVersionWriter.createVersion`) and `src/trigger/campaigns.ts` publisher wiring. The existing publisher calls `create_campaign_bundle_version`; add a durable resolution intent for pending selections at its successful bundle insertion and service that intent after the publisher returns, with a bounded recovery sweep. No fictitious setup API.
- Add studio_version source FK and v2 inputs, preserve all old digest behavior, enforce source/export/hash/profile consistency and required actual bundle/direction/slot. Existing `generateCampaignBundle` creates a real validated manifest and its publisher invokes `createCampaignVersionWriter.createVersion`; this is the concrete setup seam. Draft creation never invokes it. A later explicitly requested Campaign preparation may use its ordinary governed workflow, but selected Studio output is independently pinned as finished-source intent and resolved after the committed compatible bundle, never fed only as inspiration to its image generator. The post-commit resolution intent is durable so a worker crash cannot strand the selection; recovery replays idempotently. Conflict becomes blocked/setup-required; resolution creates fresh unreviewed output, with no copied review. Replay uses unique selection/target digest.
- Tests: pending→real compatible slot→same hash; incompatible placement/terms/copy blocks without regeneration; stale bundle, cross-tenant FK and concurrent resolution refusal; older selection superseded only explicitly; review sees selected finished poster; legacy digest golden fixtures; source reader/fake Tool Gateway consumes known imported bytes instead of direction.assetIds[0]. No live publication.
- Validation: focused domain/service/workflow/regression tests, hosted pgTAP plus first calls of changed/new table-reading functions. Gate G; Chrome review seam completed in Task 11. A database row alone is not handoff completion.

- [ ] **Task 11 — Campaign handoff UI and setup/review continuity. Depends on Tasks 8–10.**
- Create `src/components/creative-studio/campaign-handoff.tsx`/test; modify `src/components/campaigns/campaign-studio.tsx`, its tests and relevant finished-deliverable review surface discovered through current imports; extend `e2e/creative-studio.spec.ts` and `e2e/campaign-experience.spec.ts` narrowly. Add pending-selection view without changing campaign planning defaults.
- Show selected-campaign auto-link status, Attach to campaign and Create campaign draft. Draft+link action has no hidden generation. Awaiting setup remains visible with selected artwork; completing real setup binds it to review. Show conflicts as actionable setup/copy differences rather than dropping the selected asset. Fresh exact-output review uses current effective `campaign.approve` grant; publication remains separately governed.
- Tests: fresh no-bundle draft with saved selection survives reload; setup resolution visibly presents identical output hash; existing campaign immediate or pending path; approve one version does not approve sibling edit, another campaign or later copy; operator/admin actual permission behavior and viewer denial; edit after selection does not mutate campaign's pinned bytes.
- Acceptance: human-like Chrome MCP runs all handoff paths with actual stored final poster and real campaign setup where supported. If no production setup seam can create a genuine slot, retain pending state honestly and mark full handoff acceptance incomplete; do not synthesize IDs or claim done. Gate G.

- [ ] **Task 11A — Explicit Save to Asset Library. Depends on Tasks 4, 6 and 8; before Task 12.**
- Create proposed `supabase/migrations/20260921160000_studio_creative_history_sources.sql`, `supabase/tests/studio_creative_history_sources.test.sql`, `src/modules/creative-studio/application/history-filing-service.ts`/test and `src/app/api/organizations/[organizationId]/studio/versions/[versionId]/asset-library/route.ts`/test. Modify `src/domain/campaigns/creative-history.ts`, `src/modules/campaigns/application/creative-history-service.ts`, `src/modules/campaigns/infrastructure/creative-history-repository.ts`, their tests and `src/components/creative-studio/version-inspector.tsx`/test for explicit filing.
- Add immutable `studio_history_filings` receipt and `studio_version` source arm/FKs for exact selected native/export artifact; file RPC requires `studio.edit` plus existing `asset.manage`, validates folder/rights and returns same item/version on replay. Existing `asset.review` remains the only human historical review gate. Saved item starts Unreviewed; folder/title are explicit filing inputs. No automatic filing from generation, raw preview inclusion or copied Campaign verdict.
- Tests: same bytes/hash after Library reload; idempotent repeat with no duplicate; foreign folder/version/export denied; rights failure; viewer denied; unreviewed filed version absent from Approved designs until separate existing review and confirmed metadata. Hosted migration dry-run/push/pgTAP and first RPC execution required. Gate G; Chrome verifies Save to Asset Library, reload, Unreviewed status and later picker exclusion through actual persisted source.

- [ ] **Task 12 — Operational hardening, docs and complete human acceptance. Depends on Tasks 0–11A clean.**
- Create `src/workflows/creative-studio/cleanup.ts`/test; wire bounded schedule in `src/trigger/creative-studio.ts`; add `docs/verification/creative-studio-independent/final-acceptance.md`, `operations.md`; update README/module map, final verified implementation-status notes in the already-approved scoped Spec 020/019/ADR reconciliation from Task 0, permission prose, `.env.example` and narrow release configuration. Do not introduce the architectural ADR for the first time here or retroactively infer its approval; Task 0 owns its allocated path and prior approval.
- Test retention deletes only expired preview/upload/events, not final/link/continuation evidence; unknown paid outcome reconciliation, revoked provider/profile, budget exhaustion, worker restart, expired signing/context and route rollback. Pushed additive schema remains readable when admissions are disabled; existing Campaign compositor and review tests remain green.
- Final Chrome MCP verifier, independent of implementers, behaves like a user: open Studio through sidebar, pick real approved/upload/product references, type exact copy, generate without campaign, see actual progressive frames, inspect spelling/full poster, wait for stored final, reload history, marker edit using same original model context, branch older revision, download exact selected export bytes, explicitly Save to Asset Library and verify unreviewed/excluded status, generate with selected existing campaign auto-link, create new draft+link without generation, complete real setup, see same bytes in exact-output review, verify approval isolation and tenant/role denials. Capture real network/run/DB/hash evidence as well as desktop/mobile screenshots; missing prerequisite is BLOCKED, never PASS.
- Run appropriate full lint/typecheck/unit/integration/e2e gates once final code is stable; do not repeat green suites without new changes. Broad independent spec/code/security review covers all task evidence and untouched seams; fixer/re-review/browser regression repeats until all acceptance findings are clean. Preserve original PNG hashes and separate implementation screenshots. Report exact unverified limitations; no completed-task or screenshot-only claims.
- Finish only after all required tests/first staging calls/real provider and browser acceptance pass, or state the specific unfulfilled gate without claiming completion. Final handoff includes changed files, migrations applied, policy/profile versions, rollback, evidence paths and remaining user-owned deployment/push steps. No git push, external publishing, or silent auto-deployment.

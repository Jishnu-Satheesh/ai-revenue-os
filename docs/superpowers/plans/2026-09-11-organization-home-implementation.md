- **Organization Home Implementation Plan — 2026-09-11**
- **For agentic workers:** Use the `executing-plans` skill and execute the tasks in order. Implementation is assigned to a separate model. Do not start until the user authorizes this written plan; approval of the home concept is already recorded and must not be asked again. No subagents are required by this plan.
- **Goal:** Replace the current long Overview report with the organization's home: recognizable identity, recent campaigns, real creative previews, attention, goals, module destinations and activity.
- **Architecture:** Campaign-owned bounded read adapters provide safe preview DTOs. An Organizations-owned pure view builder composes them with the existing authorized Digital Twin snapshot. The current server route mounts small shadcn-based sections and reuses the existing management surface.
- **Tech stack:** Existing Next.js 16, React 19, TypeScript strict, Tailwind v4, Manrope, shadcn/Radix, Supabase session client/Storage, Zod, Vitest, Testing Library and Playwright. Add no dependency.
- **Spec:** `docs/superpowers/specs/2026-09-11-organization-home-design.md`.
- **Required companions:** `docs/superpowers/plans/2026-09-11-organization-home-data-contract.md`, `docs/superpowers/plans/2026-09-11-organization-home-visual-contract.md`, `.superdesign/organization-home/prototype.html`, `.superdesign/organization-home/source-baseline.json`, and `HANDOFF.md` in that artifact directory.
- **Status:** Proposed execution plan. Product purpose and prominent campaigns/artwork are approved. This planning turn has not changed the application. The prototype is fictional; source code and data/visual contracts govern production adaptations.

- **Global constraints**
  - Keep Overview's route, organization scoping, shared shell and sidebar unchanged. Do not rename Overview to Home in navigation.
  - Do not modify Channels, Growth Intelligence, their caches/workflows, campaign mutations, AssetWorkspace tabs, organization management authorization, `database.types.ts`, schemas, buckets, RLS or worker code.
  - No new table, migration, RPC, HTTP API, emitted business event, subscription or scheduled job. No model calls. All new operations read existing tenant-scoped state.
  - Source-owning Campaign files must not import Organizations presentation types. Define shared DTOs once in `home-preview-types.ts`; Organizations aliases them.
  - Preserve dirty files and peer commits. No stash, no `git add .`, no repository-wide formatting, no `pnpm db:types`, no local Supabase/Docker, no push. Commit only this slice's reviewed paths at task boundaries if execution was authorized; inspect staged paths first.
  - Every time a task says verify, capture command exit status. A skip due to absent authenticated E2E credentials is not a pass. Source-limited checks distinguish this slice from unrelated baseline failures.
  - No raw database rows, business descriptions, private object paths, signed URLs, prompts or credentials in logs, screenshots destined for public sharing, or handoff archives. The supplied prototype contains fictional data only.
  - Do not copy the fixture wordmark/artwork/records into application code. Production artwork is unrotated and contained. No automatically chosen ambiguous logo, fabricated goal progress or business-health score.
  - Preserve named source states: rendered is not reviewed; review-ready is not approved; approved is not published; no version is not an openable proposal; stopped generation is not running.

- **Task 0 — Reconcile the checkout and record the execution boundary**
  - Deliverable: a short execution ledger at `docs/verification/organization-home/progress.md`, listing authorized plan, HEAD, source drift, own touched paths, baseline checks and remaining gates.
  - [ ] Read `AGENTS.md`, README, relevant organization/Campaign/Asset Library context, this spec and all companions. Read the collaboration board before any file change. The prior board rows are history, not locks; preserve active work reflected in `git status`.
  - [ ] Run `git status --short` and compare the 16 source hashes in `.superdesign/organization-home/source-baseline.json`. Investigate only mismatches in files the new plan consumes. A peer change to unrelated Channel files does not block this task.
  - [ ] Check schema evidence in `.superdesign/organization-home/staging-schema-evidence.json` (schema-checked during planning). Recheck the required column subset using a **read-only hosted schema query**, not migrations or customer-row dumps. Missing fields are a concrete dependency to resolve; do not invent a column or silently switch table.
  - [ ] Record the actual shared shell: header 64px, main owns vertical scrolling, inner maximum 1440px. The standalone reference's mocked shell is illustrative; implement only within the route's existing content container.
  - Baseline command: `pnpm exec vitest run src/modules/organizations/application/overview.test.ts src/components/organizations/overview-report.test.tsx src/modules/campaigns/infrastructure/asset-preview.test.ts src/modules/campaigns/infrastructure/studio-reader.test.ts`.
  - [ ] Add the intended paths from Tasks 1–7 to the collaboration board. No production implementation happens during this task.

- **Task 1 — Define preview contracts and deterministic home composition**
  - [ ] Create `src/modules/campaigns/application/home-preview-types.ts` with `PrivatePreviewImage`, `HomeSourceResult<T>`, `CampaignHomeRecord`, `AssetHomeRecord`, and `CampaignHomeReads` exactly as specified in data-contract §1.
  - [ ] Create `src/modules/organizations/application/home-types.ts` with aliases and `HomeCampaign`, `HomeAttentionItem`, `HomeActivityItem`, `HomeGoal`, `HomeDestination`, `HomePermissions`, `OrganizationHomeView`. Import existing domain Campaign types rather than copying enum values.
  - [ ] Create `src/modules/organizations/application/home-service.ts`; export `buildOrganizationHomeView(input)` with the exact input/output in data-contract §3. Its implementation has no Supabase, Storage, process.env, clock reads or router calls. Build all routes from the explicit org ID and validated record IDs.
  - [ ] Create `src/modules/organizations/application/home-service.test.ts`; write the following behavioral tests before the function implementation.
  - [ ] Test organization A with 3 campaigns, 2 posters, 2 references, 2 active + 1 inactive branch and one org goal: 3 campaigns, 4 correctly labelled assets, 2 locations, original target and org-scoped links. No field contains a storage path.
  - [ ] Test source failures independently: campaigns fail while assets ready; posters fail while references ready; references fail while posters ready; both asset sources fail; all optional sources disabled. Assert exact failed/ready/disabled states and `assetsPartial`/`attentionIncomplete` values.
  - [ ] Test permission sets owner/admin/operator/viewer. Viewer has read destinations and no create/manage/continue-edit labels; operator gets creation/edit navigation but not approval wording or policy/lifecycle authority. Use the real permission catalogue.
  - [ ] Test gates: Campaign disabled makes no Campaign/asset action available; Growth market disabled omits Growth; integrations disabled omits Integration Hub. Channels uses verified `channel.read` and Memory uses `memory.read`.
  - [ ] Test attention: stalled campaign outranks ready-for-review regardless of update recency; blocked outranks ready-for-review; one campaign appears once even if state and generation both qualify; caption is `N shown`; zero records + failed source never says healthy/caught up.
  - [ ] Test latest-state CTA semantics: no-version campaign always points to portfolio; openable ready-for-review admin gets Review campaign; viewer/operator gets View campaign; editable draft gets Continue draft; all other openable states get View campaign.
  - [ ] Test goals: organization focus ignores a higher-priority branch goal; View goals retains it with branch scope; unknown units remain exact saved text; target 500 count stays 500 count, not 5; no percentage is produced; missing organization goal gets a neutral prompt.
  - [ ] Test activity: stable sort and prefixes distinguish same UUID across sources; unknown event names and all payload content are excluded; a campaign's updatedAt produces Campaign updated, not published; at most 5 rows. Raw actor IDs and payload sentinel strings never appear in serialization.
  - [ ] Run `pnpm exec vitest run src/modules/organizations/application/home-service.test.ts` to establish failures, implement the deterministic rules in data-contract §7, then rerun to passing. Each assertion checks user/domain outcomes, not JSX structure.
  - [ ] Review deliverable: one consistent serializable contract, no module dependency cycle, no invented finance or status claim.

- **Task 2 — Read the three recent campaigns and exact-version covers**
  - [ ] Create `src/modules/campaigns/infrastructure/home-campaign-reader.ts` and `.test.ts`. Export `readHomeCampaigns` and `HomeCampaignPersistence` exactly as data-contract §4.
  - [ ] Create `src/modules/campaigns/infrastructure/home-preview-storage.ts` and `.test.ts`. Export `HomePreviewStorage`, `signHomePreviewImages(input)` and the constant `HOME_PREVIEW_TTL_SECONDS=600`.
  - `signHomePreviewImages` input is `{ storage: HomePreviewStorage; organizationId: string; bucket: "brand-assets" | "campaign-assets"; images: readonly { id: string; path: string; alt: string; width: number; height: number }[]; now: string; correlationId: string }`; return `Promise<Readonly<Record<string,PrivatePreviewImage>>>`. Maximum input8, validate namespace/path/dimensions; output keyed by supplied record id. Deduplicate paths for signing; do not deduplicate record identity.
  - [ ] Build query-spy tests that fail unless `.limit(3)` executes on the first campaign read and `.limit(1)` on every selected-version/cover/review read. Use a fixture store with 100 historical campaigns and many versions; assert read shapes remain bounded, not just final array length.
  - [ ] Feed a wrong-tenant ID row, mismatched campaign-version ID, invalid UUID, or unknown state; expect safe source rejection and no Storage call. Never silently discard cross-tenant corruption.
  - [ ] Feed no version + expired claimed generation lease; expect `openable=false`, stalled status from `toGeneration`, no fake cover, no detail URL. Feed a live lease and assert generating, without UI invented timers.
  - [ ] Feed a current-version poster plus a newer timestamped poster from another version; only the current-version poster can become its cover. Feed no poster and one generated asset with current rejected review; expect null cover and no signing, not an older approved image.
  - For signing tests: one global Storage failure returns no images; one failed path removes only its image; shuffled returned paths map correctly; unexpected returned paths are ignored; another-org path/traversal is rejected before signing; TTL is exactly 600; URLs never appear in logged objects.
  - [ ] Implement the exact query recipe. Reuse existing `getCampaign`, `getVersion`, `latestGenerationRun`, and `toCampaignListItem`. Do not export private mapping helpers from the shared repository solely to reduce a few bounded calls.
  - Cover read errors preserve the campaign text. Core campaign-state read errors throw a domain-safe error for the loader to settle. Use bounded safe logs named `organization_home.preview_failed`, with organizationId, correlationId, source kind and safe code, never raw error payload.
  - [ ] Run `pnpm exec vitest run src/modules/campaigns/infrastructure/home-campaign-reader.test.ts src/modules/campaigns/infrastructure/home-preview-storage.test.ts src/modules/campaigns/infrastructure/studio-reader.test.ts src/modules/campaigns/infrastructure/asset-preview.test.ts`.
  - [ ] Review deliverable: three bounded campaign previews with correct lifecycle, source-version image linkage and safe fallback. No source-owned mutation/export behavior changed.

- **Task 3 — Read the creative gallery and organization logo**
  - [ ] Create `src/modules/campaigns/infrastructure/home-asset-reader.ts` and `.test.ts`, with the three exact exported readers and port from data-contract §5. Use `signHomePreviewImages` from Task 2.
  - [ ] Render candidates come from existing `campaign_poster_renders`, not raw campaign_assets or unfinished Creative History APIs. Query only selected display/linkage fields. Brand references use parent -> latest usable version -> latest review.
  - Exact poster source destination is `/organizations/{orgId}/campaigns/{campaignId}?version={bundleVersionId}`; the existing detail route's `version` parameter was verified. Do not replace it with `versionId`, `bundle`, an unsupported asset path, or a route that opens a different version.
  - [ ] Write failing tests for four rendered poster candidates + four reference parents, exact per-query bounds, valid parent-title resolution, tie-breaking, unusable version omission, archived parent exclusion and source-specific timestamps.
  - [ ] Write a review history test: older approved + latest rejected => reference absent and zero signatures for it. Absent review => Unreviewed reference; never manufacture an unreviewed review row. A poster remains Review not recorded regardless of plate review or technical verification JSON.
  - [ ] Write source isolation tests: a rejected/inaccessible parent campaign cannot leak its render bytes or title; wrong org/version link fails that source; a missing usable reference yields no record; Storage failure yields ready metadata with null image.
  - Logo tests: zero candidate => null; two active candidates => null; exactly one current approved usable logo => signed logo; current rejected/unreviewed or signing failure => null. No brand_context key is read and no synthetic logo is created.
  - [ ] Implement bounds/ordering from data-contract §§5–6. At most4 final gallery records, at most 8 candidate signatures, at most 1 logo. Keep rejected reference filtering after reading the latest verdict, without “approved-first” queries.
  - [ ] Run `pnpm exec vitest run src/modules/campaigns/infrastructure/home-asset-reader.test.ts src/modules/campaigns/infrastructure/home-preview-storage.test.ts src/modules/campaigns/infrastructure/asset-library-repository.test.ts`.
  - [ ] Review deliverable: safe recent creative previews with explicit source kind and verdict, and a deterministic non-ambiguous logo rule. No library-tab, review, archive, generation or schema changes.

- **Task 4 — Compose session-bound reads, failures and authorized destinations**
  - [ ] Create `src/modules/organizations/infrastructure/home-loader.ts` and `.test.ts`, implementing `loadOrganizationHome` from data-contract §3.
  - [ ] Instantiate Campaign-owned readers using the passed session client and the existing `createCampaignReadRepository`. Do not create a new Supabase client, read env credentials, or import a service-role client.
  - [ ] Evaluate rollout/permission gates after organization access. Set disabled source envelopes before scheduling work. Execute allowed sources independently; settle each into source states and call `buildOrganizationHomeView` from Task 1.
  - [ ] Pass required `correlationId` to each new reader's input so preview/failure logs carry the route correlation. This is part of their defined public input; use the exact same field consistently. No extra public exports in the shared Campaign barrel are required because existing routes use explicit module paths.
  - New log events: `organization_home.section_read_failed` and `organization_home.preview_failed`; include safe source (`campaigns`, `posters`, `references`, `logo`), organizationId, correlationId, elapsed milliseconds, and fixed safe code. They are diagnostic logs, not persisted business audit events or notification triggers.
  - [ ] Tests assert no calls to a gated/unauthorized source, no research/render/dispatch/campaign mutation tools called on load, all reads use the same context client+org, and error envelopes preserve other ready sections.
  - [ ] Tests assert reference permission denial cannot be bypassed through the logo reader, poster permission denial cannot be bypassed through Campaign covers, and a section error serializes no raw error message or database path.
  - [ ] Keep required Digital Twin snapshot error behavior as the route currently defines it. This slice does not split or redesign its full loader.
  - [ ] Run `pnpm exec vitest run src/modules/organizations/infrastructure/home-loader.test.ts src/modules/organizations/application/home-service.test.ts`.
  - [ ] Review deliverable: deterministic output with gate-aware bounded reads, per-source degradation, diagnostic timing and no side effects.

- **Task 5 — Build the organization home visual components**
  - [ ] Create `src/components/organizations/home/organization-home.tsx` as the composition component receiving only `view: OrganizationHomeView`.
  - [ ] Create `home-header.tsx`, `home-campaigns.tsx`, `home-assets.tsx`, `home-attention.tsx`, `home-context.tsx`, `home-activity.tsx`, `home-preview-image.tsx`, `home-refresh-button.tsx`, and `organization-home.module.css` in that directory. Group the small goal/location/destination UI in `home-context.tsx`; do not create a giant client page.
  - `HomeHeader({view})`: organization identity, logo, status/context, and allowed primary actions; Manage is a real anchor to existing management. `HomeCampaigns({section})`: first2 cards and optional third compact row using already-derived CTA/href; module empty/error/disabled states.
  - `HomeAssets({section,partial,organizationName,timeZone})`: gallery and shadcn Dialog. Store selected composite asset ID only; derive the selected item from current props so refreshed URLs replace expired ones. Close safely and restore focus if the selected asset disappears on refresh.
  - `HomePreviewImage({image,label})`: contained image/fallback with local onError; reset failed state when URL changes. An absent URL never creates a broken img request. No external placeholder service.
  - `HomeAttention({items,incomplete,readOnly})`: three bounded rows and exact incomplete/empty wording. `HomeContext({view})`: read-only goals/locations dialogs and authorized destination grid. `HomeActivity({items,timeZone})`: five bounded rows with explicit dates and safe labels.
  - `HomeRefreshButton({label})` uses `useTransition` and `router.refresh()`. Give visible pending text and disabled state. A refresh is a server read only. Preserve existing content while pending; on completion render explicit per-source failure if fresh reads fail. It does not claim a retained fresh timestamp.
  - Follow every V01–V18 requirement in the visual contract. Copy spacing/geometry from the artifact as reference, translating controls to installed shadcn primitives. Do not paste the mocked AppShell, raw prototype controls, review-state select, sample wording or artwork into production.
  - [ ] Create `organization-home.test.tsx`, `home-assets.test.tsx`, `home-campaigns.test.tsx` in the home component directory.
  - [ ] Test actual keyboard focus: open asset preview, Tab stays inside, Escape closes, focus returns; image error preserves source link; a refreshed same-ID asset uses the new URL; removed selected asset closes without throwing.
  - [ ] Test one/two/three campaign layouts by accessible content, not CSS class snapshots. Test no-version portfolio destination, preserved unknown artwork, actual viewer CTA behavior, RTL long names, and no visible create/manage action for viewer.
  - [ ] Test empty module vs failed module, partial-gallery warning with surviving items, refresh pending feedback, goals target wording and navigation IDs. Test unknown audit payload sentinel is absent in rendered text.
  - [ ] Run `pnpm exec vitest run src/components/organizations/home/organization-home.test.tsx src/components/organizations/home/home-assets.test.tsx src/components/organizations/home/home-campaigns.test.tsx`.
  - [ ] Review deliverable: responsive shadcn home with working details and navigation, no duplicate shell or main scroll container, no inaccessible thumbnail-only interactions.

- **Task 6 — Wire the route and preserve organization management**
  - [ ] Modify `src/app/(platform)/organizations/[organizationId]/overview/page.tsx`: keep `getOrganizationContext`, required `getDigitalTwin`, `RegisterRouteLabel`, existing permission derivation, then call `loadOrganizationHome` and mount `OrganizationHome`.
  - [ ] Render `OrganizationManagement` from `src/components/organizations/digital-twin-workspace.tsx` below the home for existing authorized roles, with the same organizationId, snapshot and permissions. Preserve its `#organization-management` anchor, explicit Open/Close trigger and all editor operations.
  - [ ] Remove only this route's economics, prior-window, integration snapshot and old action-queue reads that are no longer consumed. Do not delete `overview.ts`, `OverviewReport`, old charts or their tests in this slice; keeping them makes rollback narrow and avoids unrelated exports/callers.
  - [ ] Modify the existing Overview `loading.tsx` to match masthead, campaign covers, gallery and attention skeletons; keep animation modest and reduced-motion-aware. Keep the existing route `error.tsx` safe retry behavior unless its text directly names the retired economics report.
  - [ ] Create or update `src/app/(platform)/organizations/[organizationId]/overview/page.test.tsx`. Verify resolved context ID beats requested/forged ID, optional sources settle, original management role props remain exact, all read client instances match session context, and the old 30d finance/campaign-generation work is not invoked.
  - [ ] Update `README.md`'s Overview paragraph to describe organization home plus retained management. Add a short Overview home entry to `context/05-module-map.md`, linking this design and Campaign source-owned readers. Do not change domain/spec semantics.
  - [ ] Run focused home tests plus `overview.test.ts` and `overview-report.test.tsx` as regression. Verify the old components still typecheck even though the route no longer mounts them.
  - [ ] Review deliverable: the actual organization landing route serves the new home with real data and functional management, while all other workspaces remain unchanged.

- **Task 7 — Verify the implemented experience, tenant boundaries and handoff**
  - [ ] Create `e2e/organization-home.spec.ts` using existing Playwright test conventions. Unauthenticated Overview must redirect to login; nonmember organization cannot load title, records or private previews; agency-derived access works through existing effective-role context.
  - [ ] Use the existing configured staging organization and established test users. Verify owner/admin/operator/viewer flows without creating ad hoc production campaigns or using another tenant's real customer data as a fixture. Reuse repository E2E credentials when present; never print or archive them.
  - Browser acceptance at seven specified widths: accurate organization identity; three recent campaign states; exact-version poster source; image fallback; four-or-fewer gallery records; asset dialog; goals/locations; destination gates; recent activity; management editing access; no horizontal overflow; shell header and sidebar unaffected.
  - Tenant integration acceptance: using session A, verify a known permitted preview loads; a known tenant-B campaign/reference ID does not expose metadata or produce a signed URL. Database RLS plus application ID filtering both remain exercised. Schema policy inspection alone does not satisfy this gate.
  - If staging has no real qualifying artwork, capture the truthful empty/metadata state and keep the visual fixture tests separate. Do not add sample customer records to make an acceptance screenshot attractive.
  - [ ] Run `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, then `pnpm exec playwright test e2e/organization-home.spec.ts`. Record terminal exits, exact skips and unrelated failures. Do not run local Supabase or schema generation to “fix” baseline failures.
  - [ ] Run `pnpm exec prettier --check` only on this slice's changed TS/TSX/CSS files, and `git diff --check`. Apply formatting only to those files. Inspect generated `next-env.d.ts`/tsbuildinfo differences before claiming them as changes; preserve unrelated working-tree content.
  - [ ] Capture authenticated desktop/mobile images privately where appropriate. Save sanitized verification notes in `docs/verification/organization-home/`; do not package signed URLs, auth storage, raw payloads or private screenshots in the public design handoff.
  - [ ] Update execution ledger and collaboration board with implemented tasks, commit hashes, actual checks and remaining blocked acceptance. Mark the design implementation complete only when required gates pass; otherwise name the exact missing evidence.
  - Rollback: revert only the route composition/loading changes and this slice's documentation if necessary. Old OverviewReport and read functions were retained. No database rollback, destructive cleanup or worker deployment is required.

- **Blast radius and explicit implementation adaptations**
  - New exports are confined to the named home types/readers/components. Existing Campaign, Asset Library, Channel and Growth Intelligence public behavior stays intact. Session-based database and Storage policies are consumed, not modified.
  - Existing core management remains a separate collapsed surface with its original permission checks; clicking Manage scrolls there and the user opens it. The prototype destination modal must not be copied.
  - The reference has illustrative asset labels; production poster labels use campaign title + template key + script. Prototype artwork is rotated a few degrees; production previews are unrotated. The fixture logo is not a production fallback.
  - The prototype has a mock sidebar and review controls. The actual shared sidebar already determines width/collapse/mobile behavior; no sidebar addition for Asset Library is authorized. The home gallery itself provides a direct Asset Library entry.
  - Around-your-business tiles are explicitly navigation summaries. Adding counts, a calendar, measured goal progress, a personalized recents store, cross-module approval totals, or new finance summaries is a separate product decision.
  - Retain all existing domain distinctions and refusals. A missing thumbnail, rollout-disabled module, invalid source record and empty organization are four different conditions.

- **Final executor report format**
  - User-visible behavior delivered and exact commit(s), without claiming push/deployment.
  - Files changed and why; no schema/migration/worker changes.
  - Automated results with exits; authenticated acceptance and tenant-boundary evidence separately.
  - Known limitations, any deviations from the approved prototype and why, and the remaining user walkthrough if authentication was unavailable.

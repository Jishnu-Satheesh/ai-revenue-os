# Channel redesign handoff — read before execution

- Worktree: `/home/spy/Documents/ai-revenue-os/.worktrees/governed-channel-intelligence`.
- **This bundle is a proposed plan, not authorization to implement it.** The user requested planning for a successor agent. Once the user explicitly approves the plan, execute its eight tasks sequentially using executing-plans. Do not ask to redesign the page again.
- Start with `docs/superpowers/plans/2026-09-10-channels-redesign-implementation.md`, then its visual-contract companion. Both are required. The plan has 60 grouped visual/control sign-offs plus detailed V/D sections, data/permissions/error cases, exact files, interfaces, operations and commands.
- Use `.superdesign/channels/prototype.html` as the target; inspect final desktop/mobile and dialog screenshots. Read `visual-inventory.json` and `prototype-reference.css` for exact geometry, colors and SVG paths.
- Use `source-baseline.json` to detect code drift; it records HEAD `3940ab0ff9c9fd986da1c37bc590eb345b505582`. If the worktree has advanced, inspect relevant changes and preserve them. Do not reset to this commit.
- Protect inner Channel Audit and its shared ChannelSetupPanel. Protect Growth Intelligence, shared shell/root tokens, database and background-task boundaries. No migrations, new provider APIs, local Supabase or git push.
- Thirteen production adaptations are explicit: shared shell, prototype-only UI removal, real exact-window selector, reported location scope, refused-state copy, active-only portfolio, stable key/hint, independently saved mapping/alias forms, real navigation, permissions/errors, glyph fidelity, accessibility and preserved detail information. Do not copy local fixture arrays into application code.
- Planning verification: 33 current tests passed; reference prototype previously passed 26 browser checks; seven viewport inventories and seven dialogs were captured. Those are baselines, not evidence that the proposed implementation is complete. No current authenticated/staging acceptance is claimed.
- This archive contains only plans/reference material, not a repository checkout or credentials. The successor needs the actual worktree at the paths named in the plan. Source references are deliberate; don't invent missing modules from the mockup.
- Completion requires the implementation's own focused/full checks, hosted staging tenancy checks where configured, real browser evidence and explicit reporting of any skipped credential-dependent scenarios. Remove the temporary visual fixture route before the final build.
